<Page
    x:Class="LightZApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LightZApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:colorPicker="using:Color_Picker"
    mc:Ignorable="d">
    <Page.DataContext>
        <Binding Mode="OneWay" Path="Settings" Source="{StaticResource Locator}"/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button Content="Refhesh device list" Margin="0,0,0,0" Height="83" Width="303" VerticalAlignment="Top" Command="{Binding Path=RefreshDeviceListCommand}"></Button>
        <ListBox x:Name="DevicesListBox" HorizontalAlignment="Left" Width="300" Margin="0,82,0,86" ItemsSource="{Binding Path=AvailableComPorts}" SelectedValue="{Binding Path=Settings.ComPort, Mode=TwoWay}"></ListBox>
        <Button Content="Connect" Margin="0,0,0,1" Width="303" VerticalAlignment="Bottom" Height="83" Command="{Binding Path=ConnectCommand}"/>

        <colorPicker:Clr_Pckr HorizontalAlignment="Left" VerticalAlignment="Top" Height="300" Width="300" Margin="512,299,0,0" SelectedColor="{Binding SelectedColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
</Page>
