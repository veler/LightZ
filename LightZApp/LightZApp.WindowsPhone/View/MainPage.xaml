<Page
    x:Class="LightZApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LightZApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:colorPicker="using:Color_Picker"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <Binding Mode="OneWay" Path="Settings" Source="{StaticResource Locator}"/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button Content="Refhesh device list" Height="83" VerticalAlignment="Top" Command="{Binding Path=RefreshDeviceListCommand}" HorizontalAlignment="Stretch"/>
        <ListBox x:Name="DevicesListBox" Margin="0,82,0,0" ItemsSource="{Binding Path=AvailableComPorts}" SelectedValue="{Binding Path=Settings.ComPort, Mode=TwoWay}" Height="70" VerticalAlignment="Top"/>
        <Button Content="Connect" Margin="0,158,0,0" VerticalAlignment="Top" Command="{Binding Path=ConnectCommand}" HorizontalAlignment="Stretch" Height="83"/>

        <colorPicker:Clr_Pckr Margin="10,247,20,10" SelectedColor="{Binding SelectedColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
</Page>